import styled from 'styled-components';

const TodoWrapper = styled.main`
  justify-self: center;
  padding-top: 32px;
  max-width: 800px;
  min-width: 300px;
  grid-area: todos;
  width: 80%;
`;

export default TodoWrapper;
