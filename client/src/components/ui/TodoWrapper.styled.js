import styled from 'styled-components';

const TodoWrapper = styled.main`
  grid-area: todos;
  justify-self: center;
  padding-top: 32px;
  position: relative;
  max-width: 800px;
  min-width: 300px;
  width: 80%;
`;

export default TodoWrapper;
