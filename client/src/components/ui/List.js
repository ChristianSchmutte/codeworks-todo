import styled from 'styled-components';

const List = styled.aside`
  display: flex;
  flex-direction: column;
  flex-grow: 1;
  gap: 16px;
  margin-top: 16px;
  overflow-y: auto;
`;

export default List;
